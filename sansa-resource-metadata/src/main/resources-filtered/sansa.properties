# Generic sansa metadata - if desired we could track versions/build times on a
# per-module basis.
# Especially during development usually only one only builds a subset of the modules
sansa.version=${project.version}
sansa.build.timestamp=${timestamp}

